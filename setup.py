from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict a fair, alternative credit score for Grab partners (drivers/merchants) using behavioral and performance data',
    author='hrishit',
    license='',
)
